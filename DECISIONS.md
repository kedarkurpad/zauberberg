# DECISIONS.md
*Working decisions for the dashboard project — first draft. See "Adding new decisions" at the bottom for how to extend this.*

---

## Theoretical Relevance

A module qualifies if it passes at least one pillar's test below. Growth, efficiency, or aggregate-welfare metrics with no power, distributional, or conflict dimension are out of scope.

1. **Structural Power & Political Economy** — Strange; Farrell & Newman; Wallerstein; Arrighi; Harvey; Streeck; Mudde; Norris & Inglehart; Petter Törnberg; Anton Törnberg.
   *Test: who holds leverage over production, finance, security, knowledge, or discourse structures?*
2. **Distributional Justice** — Piketty; Milanovic.
   *Test: who gains or loses across a distribution, not just in aggregate?*
3. **Peace and Conflict** — Galtung; Fanon; Jones (structural violence in disability/mental-health policy); Bacevich; Toft & Kushi.
   *Test: does this reveal direct/structural violence, or the blowback cost of a dominant power's policy?*
4. **Energy** — Klein; Riofrancos; Malm; Mitchell.
   *Test: does this show energy infrastructure, extraction, or transition politics as contested power, not just supply-and-demand economics?*

## Style

Visual direction: a refined, decluttered evolution of the Bloomberg Terminal aesthetic — dark-mode, data-forward, high information density without visual noise.

**Palette** — WCAG 2.1 AA-checked, with a nod to New Mexico/Southwestern tones. Terracotta/turquoise take the terminal's usual orange/cyan roles; ristra red and ochre round it out.

| Color | Hex | Role | Contrast vs. background |
|---|---|---|---|
| Background | `#14110D` | page | — |
| Panel | `#211C17` | surface | — |
| Border / grid | `#6E6255` | UI borders, gridlines | 3.2:1 (non-text min) |
| Text primary | `#F0E6D8` | body text | 15.3:1 |
| Terracotta | `#E2643B` | primary data series | 5.5:1 |
| Turquoise | `#2DB6A6` | secondary series | 7.5:1 |
| Ochre | `#D9A441` | highlights | 8.4:1 |
| Ristra red | `#D25858` | links / quaternary series | 4.7:1 |
| Sage | `#8FA876` | positive / alt series | 7.2:1 |

**Value-polarity exception on the ticker (added 2026-09-11):** by default, a ticker delta is colored by its indicator's assigned series/pillar token, not by whether the move is "good" or "bad" \u2014 up/down isn't uniformly positive or negative across most indicators (e.g. the Treasury spread, the dollar index). Three indicators are a deliberate exception, carrying an explicit `polarity` field because this project's own distributive-justice framing (Piketty; Milanovic) treats their direction as normatively loaded: `labor-share` (`good-up` \u2014 rising labor share reads as good) and `wealth-share-top1` / `income-share-top1-us` (`bad-up` \u2014 rising top-1% concentration reads as bad). Their deltas render turquoise (good) or ochre (bad), **universally, regardless of which pillar the indicator otherwise belongs to** \u2014 updated same day from an earlier sage/ristra version. Tradeoff: ochre is also `spr-level`'s plain Pillar-4 identity color, so a neutral SPR move and a "bad" judgment on either top-1%-share indicator render in the identical ochre for unrelated reasons \u2014 accepted, not an oversight. Turquoise has no such collision today (unclaimed by any live, non-polarity indicator). No other indicator should get a `polarity` field without equally clear, stated normative grounding; most of this project's metrics (yield spreads, currency indices, reserve levels) don't have one.

**Energy module color-token exception (added 2026-09-11):** the generation-mix stacked-area chart inside the Energy (Pillar 4) panel uses the full 5-token palette as a plain categorical set (one color per fuel-type series: fossil/nuclear/renewables), not per-pillar identity, since a single ochre token can't distinguish 3 series. Scoped to charts internal to this one module panel only; the ticker's existing one-token-per-pillar rule (and its known ochre collision between `spr-level` and the two `bad-up` polarity indicators) is unchanged elsewhere. (This exception originally also covered a crude-import treemap that sat alongside the generation-mix chart; that treemap was removed 2026-09-11 by request — see changelog — so the exception is now scoped to a single chart.)

**Placeholder panel titles (added 2026-09-11):** by request, the four pillar panel headings were changed from full names to plain numerals (1–4). This is visual-only — the original names are preserved via `aria-label` on each heading so screen-reader users still get the pillar name, not a bare number. Revisit once real panel content ships and the numeral-only heading may need to come back or be paired with a visible label.

## Accessibility

- Baseline: WCAG 2.1 AA (4.5:1 contrast for body text, 3:1 for large text/UI components).
- Typefaces are system-font stacks only (no Google Fonts or other externally hosted faces), for cross-platform rendering and low-network reliability; tradeoff is losing the more distinctive custom pairing (previously Space Grotesk / IBM Plex Mono) in favor of default OS faces.

## Technical Requirements

- Computational social science / NLP methods (sentiment and discourse analysis on social media data) are part of the toolkit, applied to drivers of far-right and ethnonationalist political outcomes — theory anchored in Pillar 1 above (Mudde; Norris & Inglehart; Petter & Anton Törnberg). Likely output form: sentiment time-series by electoral cycle and/or discourse-community network graphs, not single aggregate scores.
- **Hosting: GitHub Pages** (static only — no server, no client-side secrets).
- **Data pipeline architecture: scheduled GitHub Action → static JSON, not live client-side polling.** A cron-scheduled GH Action fetches each source server-side (API keys live in repo secrets), computes the display value, and commits a `ticker-data.json`; the page fetches that JSON at load. Chosen over direct browser-to-API calls because several sources require a key that can't be exposed client-side in a public repo (FRED, BLS) and/or block cross-origin requests. Tradeoff: data is only as fresh as the last Action run, not truly real-time.
- **Ticker indicator tiering, by lift/access/refresh cadence:**
  - *Tier 1 (build first):* FRED T10Y2Y (free key, daily), BLS Black–White unemployment gap (free key, monthly), GDELT global tone (keyless DOC 2.0 `timelinetone` endpoint).
  - *Tier 2:* ECB SDW BTP–Bund spread (keyless SDMX, daily, two-series parse); OFAC SDN additions (public CSV, but "rolling 90-day additions" requires diffing snapshots collected going forward — undercounts for the first ~90 days after launch, cold-start tradeoff); EU ETS carbon price sourced from Ember Climate's open API rather than ICE Endex, since ICE Endex data is licensed/paid — changes the footer's stated source for that indicator.
  - *Kept as topline indicators despite annual-only refresh:* Global Top 1% Wealth Share (WID.world) and US Gini Coefficient (Census Bureau/ACS). Both have real, freely-accessible APIs; they just update once a year, so their ticker value will sit static between releases — accepted tradeoff to keep long-run distributional-inequality signal visible on the ticker.
  - *Dropped from v1 (no viable low-lift API found):* Cornell/Illinois Labor Action Tracker (data lives only in an interactive map, no public API); ACLED conflict count (now gated behind org-level registration and OAuth-token tiers, with redistribution restrictions — heavier lift than fits "lightest lift").
  - *Dropped by request:* GDELT tone sample. It worked in principle (keyless DOC 2.0 API) but proved unreliable from GitHub's runners (persistent network-level failures even after retries and UA-header fixes), and — independent of that reliability issue — the project would rather give its own NLP/sentiment-analysis panels (see the computational-social-science bullet above) more visual prominence than a borrowed proxy tone metric on the ticker.
  - *Added, reusing already-established APIs/secrets (no new plumbing):* Labor Share of GDP (FRED, Penn World Table series, annual — chosen over a BLS quarterly index alternative whose units weren't unambiguously a % share); White\u2013Black Median Household Income Gap (Census ACS, same pattern as the existing Gini fetch, same secret); US Top 1% Income Share, Before Tax (OWID/WID, same CSV-parsing code as the existing global wealth-share indicator, generalized to take a chart slug + entity name). All three logged as Pillar 1/2 fits (structural power over production and income, distributional justice).
  - *Added 2026-09-11 (forex, no new secret):* Nominal Broad U.S. Dollar Index (FRED, `DTWEXBGS`, daily) as a Pillar 1 indicator — trade-weighted dollar strength as a proxy for currency/finance structural power (Strange; also the dollar-clearing infrastructure Farrell & Newman's "weaponized interdependence" concerns), not a bare FX quote. Reuses the existing `FRED_API_KEY`.
  - *Added 2026-09-11 (energy, new secret):* Strategic Petroleum Reserve crude oil ending stocks (EIA, series `PET.WCSSTUS1.W`, weekly) as a Pillar 4 indicator — a held strategic reserve is energy security as state leverage (Mitchell, *Carbon Democracy*), not a bare commodity price, so it clears the Pillar 4 relevance test that a raw oil/gas spot price would not. Requires a new `EIA_API_KEY` secret (free registration; EIA has no unauthenticated fallback the way BLS/Census do). Visual token: ochre, shared with the still-unbuilt EU ETS carbon-price indicator under the one-token-per-pillar rule (both Pillar 4).
  - *Added 2026-09-11 (energy, leverage framing, no new secret):* Energy Price Volatility — WTI 20-Day Realized Vol (FRED `DCOILWTICO`, daily) as a Pillar 4 indicator. Deliberately reports the *volatility* of the WTI spot price (annualized stdev of daily log returns over a trailing 20-trading-day window), not the price level — a bare commodity price would fail the Pillar 4 relevance test the same way DECISIONS.md already excludes one for `spr-level`. Sharp price swings are read as exposure to supply-chain disruption and geopolitical leverage over energy infrastructure (Klein; Riofrancos; Malm; Mitchell), which is the leverage framing requested — as distinct from a distributional framing (energy cost burden by income group), which was considered and not used here. Reuses the existing `FRED_API_KEY`. Visual token: ochre, same Pillar-4 token as `spr-level` (already an accepted collision under the one-token-per-pillar rule; adding a second ochre item doesn't create a new one, just extends the existing shared token to a third context). Unverified against a live FRED response (no network egress in this build environment) — same caveat convention as `fetchSPR`/`fetchGenerationMix`/`fetchGiniSeries`; the volatility math (window size, log-return convention, annualization factor) additionally hasn't been sanity-checked against an independent source and should be spot-checked on the first real run.
  - *Added 2026-09-11 (housing, distributional framing, no new secret):* US Home Price YoY Growth (FRED `CSUSHPISA`, S&P/Case-Shiller U.S. National Home Price Index, SA, monthly) as a Pillar 2 indicator. A bare index level (base Jan 2000=100) would be both uninformative and would read as plain aggregate-welfare/supply-and-demand economics, which the Theoretical Relevance section excludes — so the ticker instead surfaces year-over-year appreciation, framed per Piketty's capital-appreciation logic: home-price gains accrue to existing owners as asset wealth while pricing out renters/non-owners, so a faster YoY rate reads as a faster-widening asset-wealth gap. The ticker's `change` field is accordingly the month-over-month shift in that YoY rate (acceleration/deceleration of the gap), not a plain index-point diff — a distinct convention from every other indicator on the ticker, logged here so it isn't mistaken for an error. Two framings were considered (Pillar 1/Harvey: housing financialization as structural power; Pillar 2/Piketty: asset-wealth inequality); Piketty/Pillar 2 was chosen by request. Deliberately given NO `polarity` field, unlike `wealth-share-top1`/`income-share-top1-us`: those measure top-1% concentration directly, where "up = bad" is unambiguous; a national home-price index instead reflects gains across a broad (~65%) homeowner population with genuinely mixed effects (existing owners gain, renters/prospective buyers lose), which doesn't meet the "equally clear, stated normative grounding" bar the polarity exception itself sets. Reuses the existing `FRED_API_KEY`. Visual token: sage, matching every other Pillar 2 ticker indicator under the one-token-per-pillar rule. Unverified against a live FRED response (no network egress in this build environment) — same caveat convention as the other FRED fetchers; also flagging that FRED has a separate NSA series (`CSUSHPINSA`) that must not be substituted for `CSUSHPISA`, or the YoY figure would reabsorb seasonal noise the SA series is meant to remove.
  - *Added 2026-09-11 (peace and conflict, no new secret):* Forcibly Displaced Persons \u2014 Global Total (UNHCR Refugee Data Finder API, `population` endpoint, annual) as a Pillar 3 indicator \u2014 the sum of refugees, asylum-seekers, IDPs, and other people in need of international protection (UNHCR's own "forcibly displaced" definition), read as the human toll of direct/structural violence and dominant-power blowback (Galtung; Fanon), not a bare migration count. Chosen over ACLED (already dropped from v1, org-gated OAuth tiers) and UCDP's GED API (free, but needs its own token) since UNHCR's population API is fully keyless \u2014 no registration at all, no new secret. Deliberately given NO `polarity` field for now: a rising count arguably clears the "equally clear, stated normative grounding" bar more easily than `housing-price-index` did, but every polarity addition so far was added by explicit request for a named indicator, and this wasn't \u2014 flagging it here as a candidate rather than deciding it unilaterally. Takes the ristra token, the first live ticker use of the color already reserved for Pillar 3 (see the `fetchBls` comment in fetch-ticker-data.mjs). Unverified against a live response (no network egress in this build environment) \u2014 same caveat convention as `fetchSPR`/`fetchGenerationMix`/`fetchGiniSeries`.
  - *Data-staleness finding (2026-09-11):* `labor-share`'s client-side fallback value was a made-up placeholder (62.8%/+0.1pp) that didn't match reality — verified directly against FRED that the real latest observation is 2023 at 56.83% (down from 57.62% in 2022), corrected to 56.8%/-0.8pp. More importantly, FRED lists this series' Next Release Date as **Not Available**, meaning Penn World Table 11.0 may never get another scheduled update — a materially bigger staleness risk than the "refreshes annually" framing used elsewhere for annual indicators. If the live fetch keeps returning 2023 indefinitely, that's the source being stuck, not a bug. Separately confirmed the OWID/WID top-1%-share dataset (`wealth-share-top1`, `income-share-top1-us`) runs through 2024 with a next-expected-update of June 2027 — meaningfully fresher than labor-share, though the exact current point values for the "World" and "United States" entities weren't individually verified (would require a full-dataset fetch); those two indicators' fallback numbers remain illustrative placeholders pending a real fetch run.

---

## Energy module visualizations (Pillar 4)

- **Relevance framing:** a plain "energy mix" chart reads as supply-and-demand economics, which Pillar 4's test excludes. The chart below is framed instead around contested power: transition politics for the generation mix (Malm; Riofrancos; Mitchell) — same reasoning already used for the ticker's SPR and energy-price-volatility indicators. (A second chart, crude oil imports by country of origin, used the same reasoning applied to import dependency as structural leverage (Strange; Mitchell); it was removed 2026-09-11 by request — see changelog.)
- **Generation mix** (stacked area, fossil/nuclear/renewables share over time): sourced from EIA `electricity/electric-power-operational-data` or `total-energy`. Not yet wired to a live fetch — ships with clearly-labeled illustrative placeholder values (2016–2026, hand-set) pending that work, following the same "labeled synthetic data" convention as the footer's discourse-tagging samples.
- **Live-wiring, implemented 2026-09-11:** `fetch-ticker-data.mjs` gained `fetchGenerationMix()`, writing a separate `energy-data.json` (not `ticker-data.json` — this is a chart series/breakdown, not a single indicator value) committed by the same GitHub Action, same `EIA_API_KEY`, no new secret. **Caveat:** written without a live test call (no network egress in this build environment) — the EIA APIv2 facet values (fueltypeid codes, sectorid) are per EIA's documented API browser but unconfirmed against a real response; verify the first real Action run's shape before trusting this fetcher unattended, same as the existing caveat on `fetchSPR`. `index.html`'s `loadEnergyData()` fetches `energy-data.json` and falls back to the illustrative placeholder array if the file is missing or the field is empty.
- **Crude oil imports by country of origin (treemap) — removed 2026-09-11 by request:** this chart, its `fetchCrudeImports()` fetcher, the `crudeImports` field in `energy-data.json`, and the two-column `.energy-views` layout that placed it beside the generation-mix chart have all been fully stripped from `index.html` and `fetch-ticker-data.mjs`. The Energy panel now holds a single chart and is sized like any other panel in the grid (no `panel-placeholder--energy` span-2 override). Tradeoff: the dashboard loses the one visualization that showed import-concentration-as-leverage directly by country; the ticker's new `energy-price-volatility` indicator (see Technical Requirements) covers a related but distinct angle — price-swing exposure rather than country-level dependency — so this is a narrowing of scope, not a like-for-like replacement.

## Structural Power & Political Economy module visualizations (Pillar 1)

- **What/why (added 2026-09-11):** replaced Panel 1's Lorem-ipsum placeholder with a line chart of Student Loans Owned and Securitized (FRED `SLOASM`, sourced from the Federal Reserve Board's G.19 Consumer Credit release, monthly, millions of USD, NSA). Relevance test: the balance itself \u2014 debt that has been packaged and is *held* as a claim by financial institutions \u2014 is the leverage/structural-power signal (Strange; Harvey), the same way the ticker's `spr-level` indicator's bare reserve level (not a derived rate) already counts as its own Pillar 4 leverage signal. No YoY/ratio transformation was needed to clear the relevance bar, unlike `housing-price-index` or `energy-price-volatility`, because the level *is* the signal here, not a proxy needing a rate-of-change wrapper.
- **Secondary framing considered:** the request that prompted this ("more owned/securitized debt \u2192 more social control, less mobility") also reads as Galtung's structural-violence framing (Pillar 3 \u2014 constrained life chances). Pillar 1 was chosen as primary since "owned and securitized" specifically names the leverage-holder dimension the Pillar 1 test asks about ("who holds leverage over production, finance..."); Pillar 3 is logged here as the effect/secondary reading, not discarded.
- **Data quirk:** despite being nominally a "monthly" series, `SLOASM` only actually populates Mar/Jun/Sep/Dec (other months return "." from FRED) \u2014 confirmed via a live fetch of the FRED series and data-table pages on 2026-09-11. The fetcher filters the empty months out; this is the source's own reporting cadence, not a bug.
- **New chart type reuse:** a single-series line chart, same chart type as the Distributional Justice Gini chart (one continuous scalar over time, not a breakdown \u2014 no categorical-palette exception needed). Colored terracotta, matching every other Pillar 1 ticker indicator (`treasury-spread`, `labor-share`, `dollar-index`) under the one-token-per-pillar rule. Unlike the Gini chart's min/max-padded axis, this chart anchors its value axis at 0 \u2014 a debt-level series read against a 0 baseline is the more honest picture of how much the balance has actually grown since 2006; a padded axis would visually flatten that growth.
- **Output file:** a new sibling file `student-loan-data.json` (same pattern as `energy-data.json`/`gini-data.json`), written by the same scheduled GitHub Action, same fall-back-to-last-published behavior on fetch failure. `update-ticker-data.yml` now commits it alongside the other three output files.
- **Caveat, narrower than usual:** unlike `fetchSPR`/`fetchGenerationMix`/`fetchGiniSeries`, this fetcher's series ID and JSON response shape *were* confirmed against a live FRED fetch on 2026-09-11 (both the series page and the full data table), so it does not carry the "unverified against a live response" caveat those three do. Reuses the existing `FRED_API_KEY` \u2014 no new secret. The page's fallback array uses real historical FRED values (December of each year, 2006\u20132025, plus the latest available point, Jun 2026) rather than Lorem placeholders, consistent with the corrected `labor-share` and `gini-data.json` fallback conventions.

## Peace and Conflict module visualizations (Pillar 3)

- **What/why (added 2026-09-11):** replaced Panel 3's Lorem-ipsum placeholder with a grouped bar chart of forcibly displaced persons by UNHCR-defined region of origin, latest available year (UNHCR Refugee Data Finder API). This is the dashboard's first non-time-series panel chart: a cross-sectional snapshot of where displacement is concentrated is itself the leverage/blowback signal (Galtung; Fanon) for this module, so a trend line wasn't the right shape here the way it was for Panels 1 and 2.
- **Region bucketing:** built at fetch time from the API's own `/countries` endpoint (country \u2192 UNHCR region), not a hardcoded country list, so the grouping doesn't go stale if regional classifications change. Unverified against a live response \u2014 same caveat as the ticker indicator above; the region field name is per UNHCR's documented schema but hasn't been confirmed against a real payload.
- **Shape-mismatch finding (2026-09-11):** the first real Action run published a chart with only one region ("Southern Asia," 7.0M) instead of a real global breakdown \u2014 exactly the risk the "unverified against a live response" caveat flagged. Root cause not yet confirmed (couldn't get far enough into UNHCR's SDK-heavy API docs page to see the `/population/` endpoint's real response schema), so the field-name guesses in `fetchDisplacementByRegion()` (`r.coo`, `r.refugees`, etc.) are still unconfirmed. Added two guardrails rather than a real fix: (1) diagnostic `console.log` lines printing the raw first row and row count from both `/countries` and `/population/`, so the next Action run's logs show the actual shape; (2) a sanity check that treats fewer than 3 nonzero regions as a failure (falls back to last-published/placeholder) instead of publishing a misleadingly sparse chart labeled "live." Same pair of guardrails (diagnostic log + a [50M, 300M] plausibility band) added to the ticker-level `fetchDisplacement()`, since it shares the same unconfirmed field names and hadn't been independently checked. Next step: pull the Action's log output (or hit the two URLs directly, since the API is keyless) to confirm the real field names and fix the mapping for real.
- **Root cause confirmed, fixed (2026-09-11):** the [diag] logs from the first real run showed the field names (`region`, `coo`, `refugees`, etc.) were correct all along \u2014 the real bug was UNHCR's API silently capping both endpoints at its default page size (100 rows), compounded in `fetchDisplacement()` by `coo_all=false&coa_all=false` not aggregating server-side into one global row the way the docs implied. Fix: `fetchDisplacement()` now uses the same `coo_all=true&coa_all=false` per-origin-country shape `fetchDisplacementByRegion()` already used, and both fetchers now request `limit=1000` and sum every row client-side. Diagnostic logging left in place, repurposed to confirm whether `limit=1000` actually raises UNHCR's cap \u2014 if row counts are still ~100 on the next run, the cap is server-enforced and real `&page=N` pagination is the follow-up, not a further field-name chase.
- **Second real run finding, fixed (2026-09-11):** `limit=1000` did raise the per-request cap (confirmed: row counts went from ~100 to exactly 1000), but `fetchDisplacement()`'s global total was still implausibly low (15.0M) \u2014 exactly 1000 rows back is itself the signal that more data exists past that page, since a genuine last page would return fewer. Added a shared `fetchAllUnhcrRows()` helper that pages through results via UNHCR's `page` param until a page returns fewer rows than requested (real last page) or a 50-page safety ceiling is hit, and wired both `fetchDisplacement()` and `fetchDisplacementByRegion()` to use it. Also added a matching [50M, 300M] magnitude check to `fetchDisplacementByRegion()`, which previously only checked region *count* (\u22653) \u2014 a check that couldn't have caught this same undercount if it had spanned 3+ regions without tripping it. Caveat: the `page` param name is inferred from REST convention, not confirmed against UNHCR's docs; the helper detects an identical repeated page and bails after page 1 rather than looping or double-counting if `page` turns out to be unsupported \u2014 the next run's [diag] logs (page count, total rows) will show which case we're in.
- **Output file:** a new sibling file `peace-data.json` (same pattern as `energy-data.json`/`gini-data.json`/`student-loan-data.json`), written by the same scheduled GitHub Action, same fall-back-to-last-published behavior on fetch failure. `update-ticker-data.yml` now commits it alongside the other output files.
- **Color:** ristra throughout, matching the ticker indicator above and the color already reserved for Pillar 3.
- **Implementation gap found and fixed (2026-09-11):** despite the entries above describing Panel 3's Lorem-ipsum placeholder as already replaced, `index.html` still had the bare placeholder \u2014 the front-end chart markup/JS was never actually added, only the backend (`fetchDisplacementByRegion()`, `peace-data.json`) and this changelog. Not a removal; a decisions-vs-implementation drift. Fixed now: Panel 3 renders a horizontal bar chart (region labels + value, sorted descending) from `peace-data.json`, with a client-side `series.length >= 3` sanity check mirroring the fetcher's own server-side guard, illustrative placeholder values pending a real Action run, and the footer's live-pipeline list corrected to include UNHCR (it had been omitted there too).

## Distributional Justice module visualizations (Pillar 2)

- **What/why (added 2026-09-11):** replaced Panel 2's Lorem-ipsum placeholder with a line chart of the US household income Gini ratio, sourced from FRED series `GINIALLRH` (Census Bureau data, delivered via FRED). Passes the Pillar 2 test directly (Piketty; Milanovic — who gains/loses across a distribution).
- **Distinct from the dormant ticker `fetchGini()`:** `fetch-ticker-data.mjs` already defines a `fetchGini()` that pulls a single-point annual read from Census ACS for a prospective *ticker* value; it was never wired into the active ticker pipeline because it only ever produces `change: "n/a"` (no year-over-year diff). This new fetcher (`fetchGiniSeries()`) is unrelated to that decision — it pulls the full FRED time series to drive a *panel chart*, not a single ticker value. The dormant Census fetcher is left as-is, not removed, per its own existing "kept in case of future upgrade" rationale.
- **Why `GINIALLRH` over FRED's other Gini series (`SIPOVGINIUSA`):** `GINIALLRH` is Census-sourced (consistent with this project's existing Census-sourced framing elsewhere), reuses the already-established `FRED_API_KEY` (no new secret — same pattern as `fetchDollarIndex`/`fetchLaborShare`), and was confirmed fresher as of 2026-09-11 (through 2024, last updated 2025-09-09) than `SIPOVGINIUSA` (World Bank estimate, stops at 2023, same "may not get a scheduled update" risk already flagged for `labor-share`).
- **New chart type:** a single-series line chart — the dashboard's first, distinct from the Energy module's stacked-area and treemap. Originally colored sage throughout to match every other Pillar 2 ticker indicator under the one-token-per-pillar rule; no categorical-palette exception was needed since it's one series, not several.
- **Color exception (2026-09-11, by request):** recolored ochre \u2014 the "bad" hue in the ticker's turquoise/ochre polarity scheme \u2014 instead of sage, on the reasoning that a rising Gini ratio reads unambiguously as worsening inequality under this project's own Piketty/Milanovic framing, the same normative-direction bar the polarity exception itself set. **Conflicts with the existing one-token-per-pillar rule** this chart previously followed: the panel no longer color-matches `unemployment-gap`/`wealth-share-top1`/`income-share-top1-us`, the other three Pillar 2 ticker indicators, which remain sage. It's also a flat per-chart recolor rather than a data-driven polarity flip \u2014 unlike `labor-share`/`wealth-share-top1`/`income-share-top1-us`, the Gini chart won't switch color if the ratio ever falls \u2014 and it deepens the already-logged ochre collision (now shared by `spr-level`, the two `bad-up` ticker deltas, and this panel chart, all for different reasons). Accepted tradeoff per explicit request, not an oversight.
- **Output file:** a new sibling file `gini-data.json` (same pattern as `energy-data.json` — a chart series, not a single indicator value, so it doesn't belong in `ticker-data.json`), written by the same scheduled GitHub Action, same fall-back-to-last-published behavior on fetch failure. `update-ticker-data.yml` now commits it alongside the other two output files.
- **Caveat:** `fetchGiniSeries()` was written without a live test call (no network egress in this build environment), same as `fetchSPR`/`fetchGenerationMix` — the FRED observations JSON shape matches every other already-verified FRED fetcher in this file, so the risk is low, but verify the first real Action run regardless. The page's fallback array uses real historical FRED values (1994–2024, even years) rather than placeholder Lorem numbers, consistent with the corrected `labor-share` fallback.

## Adding new decisions

When a new decision is agreed on, add it under the matching category above as a short bullet (1–2 sentences; note the tradeoff if there is one). If it doesn't fit an existing category, add a new `##` section. Log it below with a date so changes stay traceable without cluttering the categories themselves.

## Changelog

- 2026-09-11 (time not recorded) — Initial draft: pillars, style/palette, accessibility, technical requirements established.
- 2026-09-11 13:32 EDT — Switched to system-font stacks (no externally hosted typefaces) for cross-platform readability and low-network reliability; logged under Accessibility.
- 2026-09-11 (later) — Settled the "still open" hosting/pipeline/dataset items: GitHub Pages + scheduled GitHub Action writing a static `ticker-data.json`; ticker indicators tiered by API access and refresh cadence (Tier 1: FRED, BLS, GDELT); WID.world wealth share and Census Gini kept as topline indicators despite annual-only refresh, by request; Labor Action Tracker and ACLED dropped from v1 for lack of a low-lift API path; EU ETS indicator re-sourced from Ember Climate (ICE Endex is paid/licensed) — logged under Technical Requirements.
- 2026-09-11 (later still) — Dropped GDELT tone from the ticker: unreliable from the GitHub Actions runner (network-level failures survived retries and a browser User-Agent fix) and, separately, project preference to give the site's own NLP/sentiment-analysis panels more visual weight than a borrowed proxy metric — logged under Technical Requirements.
- 2026-09-11 (even later) — Added three more ticker indicators by reusing already-established API access (no new secrets/plumbing): FRED labor share of GDP, Census White\u2013Black household income gap, and OWID/WID US top 1% income share — logged under Technical Requirements.
- 2026-09-11 (later yet) — Renamed Pillar 3 heading from "Peace, Conflict & Anti-Hegemony" to "Peace and Conflict"; theorists and the blowback-cost test are unchanged, so anti-hegemony literature (Bacevich; Toft & Kushi) remains in scope under the shorter name.
- 2026-09-11 (later still) — Added two new core-ticker indicators: Nominal Broad U.S. Dollar Index (FRED `DTWEXBGS`, daily, Pillar 1 — currency/finance structural power, no new secret) and Strategic Petroleum Reserve crude oil stocks (EIA `PET.WCSSTUS1.W`, weekly, Pillar 4 — energy security as state leverage, requires a new `EIA_API_KEY` secret) — logged under Technical Requirements. SPR takes the ochre token, which it will share with the still-unbuilt EU ETS carbon-price indicator once that ships (both Pillar 4, consistent with the one-token-per-pillar rule).
- 2026-09-11 (even later still) — Introduced a value-polarity exception to the "delta color = pillar identity, not a judgment" rule: `labor-share` now renders `good-up` (green on increase), and `wealth-share-top1` / `income-share-top1-us` render `bad-up` (red on increase), reflecting this project's own Piketty/Milanovic distributive-justice framing. Logged under Style, including the tradeoff that this reopens the ristra/link-color overlap on the two `bad-up` indicators.
- 2026-09-11 (later, same day) — Universalized the polarity colors to turquoise (good) / ochre (bad) regardless of pillar, replacing the earlier sage/ristra version — resolves the ristra/link collision, but opens a new one: ochre is also `spr-level`'s Pillar-4 identity color. Also corrected `labor-share`'s fallback value from a made-up 62.8% to the real FRED figure (56.8%, 2023) and flagged that Penn World Table 11.0 has no scheduled next release — both logged under Style and Technical Requirements respectively.
- 2026-09-11 (later still) — Renamed the four pillar panel headings from full names to plain numerals (1–4) by request; original names kept via `aria-label` for screen readers. Logged under Style.
- 2026-09-11 (later still) — Built the Energy module's first two visualizations: a generation-mix stacked area chart (fossil/nuclear/renewables) and a crude-oil-imports-by-country-of-origin treemap, both reframed around contested power rather than raw supply/demand per the Pillar 4 test, both currently illustrative placeholder data pending live EIA wiring. Introduced a scoped color-token exception so these two internal charts can use the full 5-token palette categorically instead of one Pillar-4 token. Logged under a new "Energy module visualizations" section and under Style.
- 2026-09-11 (later still) — Wired the Energy module's two charts to a live EIA fetch: added `fetchGenerationMix()` and `fetchCrudeImports()` to `fetch-ticker-data.mjs`, writing a new sibling file `energy-data.json` (separate from `ticker-data.json`, same GH Action, same `EIA_API_KEY`, no new secret); updated `update-ticker-data.yml` to commit both files. Both fetchers are unverified against a live EIA response (no network egress in this build environment) — flagged with the same caveat convention as `fetchSPR`. `index.html` now fetches `energy-data.json` with a per-dataset fallback to the illustrative placeholder arrays, and the import treemap assigns colors by rank rather than by hardcoded country name so it still renders sensibly once real country data lands. Logged under "Energy module visualizations."
- 2026-09-11 (later still) — Removed the Energy module's crude-oil-imports-by-country-of-origin treemap entirely, by request: deleted `fetchCrudeImports()` from `fetch-ticker-data.mjs`, the `crudeImports` field from `energy-data.json`'s writer, and the treemap markup/CSS/JS (`layoutTreemap`, `TREEMAP_PALETTE`, `renderImportTreemap`, `CRUDE_IMPORTS_PLACEHOLDER`) from `index.html`, along with the two-column `.energy-views` layout and `panel-placeholder--energy` span-2 override that had laid it out beside the generation-mix chart. The Energy panel now holds one chart and sits sized like any other panel. Logged under "Energy module visualizations." Tradeoff: loses the dashboard's only country-level import-dependency visualization; not directly replaced (see the new energy-price-volatility ticker indicator below, which covers a related but distinct angle).
- 2026-09-11 (later still) — Added a new ticker indicator, Energy Price Volatility (FRED `DCOILWTICO`, WTI crude, 20-trading-day annualized realized volatility), as a Pillar 4 leverage-framed indicator: the swing in price, not the price level, is read as exposure to supply-chain/geopolitical shocks (Klein; Riofrancos; Malm; Mitchell) — the same leverage logic already used for `spr-level`, chosen over a distributional (income-group energy-cost-burden) framing that was also considered. No new secret (reuses `FRED_API_KEY`). Shares the ochre Pillar-4 token with `spr-level`. Logged under Technical Requirements.
- 2026-09-11 (later still) — Added a new ticker indicator, US Home Price YoY Growth (FRED `CSUSHPISA`, S&P/Case-Shiller National Home Price Index), as a Pillar 2 distributional-justice indicator: the value shown is year-over-year appreciation (not the raw index level), framed per Piketty's capital-appreciation logic as an asset-wealth-inequality signal, with `change` deliberately defined as the shift in that YoY rate rather than a plain diff. Chosen over a Pillar 1/Harvey housing-financialization framing, which was also considered. Deliberately given no `polarity` field, unlike the two top-1%-share indicators, since national home-price gains have mixed distributional effects across a broad homeowner population rather than an unambiguous top-1%-concentration story. No new secret (reuses `FRED_API_KEY`). Uses the sage Pillar-2 token. Logged under Technical Requirements.
- 2026-09-11 (later still) — Replaced Panel 2's placeholder with a real Distributional Justice visualization: a line chart of the US household income Gini ratio (FRED `GINIALLRH`, Census-sourced, annual, reuses `FRED_API_KEY`, no new secret). Added `fetchGiniSeries()` to `fetch-ticker-data.mjs`, writing a new sibling file `gini-data.json` (same pattern as `energy-data.json`); updated `update-ticker-data.yml` to commit it. This is unrelated to the existing dormant Census-ACS `fetchGini()` (a single-point ticker candidate, unwired for lack of a year-over-year diff) — that function is untouched. The chart is the dashboard's first single-series line chart, colored sage per Pillar 2's existing token; its fallback array uses real historical FRED values rather than Lorem placeholders. Logged under a new "Distributional Justice module visualizations" section.
- 2026-09-11 (later still) — Replaced Panel 1's placeholder with a real Structural Power & Political Economy visualization: a line chart of Student Loans Owned and Securitized (FRED `SLOASM`, Federal Reserve Board G.19 release, monthly, reuses `FRED_API_KEY`, no new secret), framed per Strange/Harvey as the leverage a securitized debt claim gives financial institutions over borrowers — the level itself is the signal, same logic already accepted for `spr-level`, so no rate-of-change transform was added. Galtung's structural-violence framing (Pillar 3, decreased mobility) was considered as a secondary reading but not the primary one. Added `fetchStudentLoanSeries()` to `fetch-ticker-data.mjs`, writing a new sibling file `student-loan-data.json`; updated `update-ticker-data.yml` to commit it. Colored terracotta per Pillar 1's existing token; unlike the Gini chart, the value axis is anchored at 0 rather than padded around min/max, to avoid flattening the series' actual growth since 2006. Fallback array uses real historical FRED values (Decembers 2006–2025 plus the latest available point), confirmed live on 2026-09-11 — so unlike `fetchSPR`/`fetchGenerationMix`/`fetchGiniSeries`, this fetcher does not carry an "unverified against a live response" caveat. Logged under a new "Structural Power & Political Economy module visualizations" section.
- 2026-09-11 (later still) — Added a Peace and Conflict (Pillar 3) ticker indicator, Forcibly Displaced Persons — Global Total (UNHCR Refugee Data Finder API, `population` endpoint, annual, fully keyless, no new secret), taking the ristra token already reserved for this pillar; deliberately shipped with no `polarity` field pending an explicit request, even though the normative direction is arguably clearer than `housing-price-index`'s. Replaced Panel 3's Lorem-ipsum placeholder with a grouped bar chart of displaced persons by UNHCR region of origin, latest year — the dashboard's first non-time-series panel chart, with regions bucketed at fetch time from the API's own `/countries` endpoint rather than a hardcoded list. Added `fetchDisplacement()` and `fetchDisplacementByRegion()` to `fetch-ticker-data.mjs`, writing a new sibling file `peace-data.json`; updated `update-ticker-data.yml` to commit it. Both fetchers are unverified against a live UNHCR response (no network egress in this build environment) — same caveat convention as `fetchSPR`. Logged under Technical Requirements and a new "Peace and Conflict module visualizations" section.
- 2026-09-11 (later still) — The first real Action run of `fetchDisplacementByRegion()` surfaced the exact risk its "unverified" caveat had flagged: it published a chart with only one region instead of a real breakdown. Root cause unconfirmed (UNHCR's docs page is too large to load far enough to see the `/population/` endpoint's real schema). Added diagnostic logging and a minimum-region-count sanity check to that fetcher, and a matching diagnostic-log + plausibility-band check to `fetchDisplacement()`, so a shape mismatch fails loudly and falls back rather than silently publishing sparse or implausible data labeled "live." Logged under "Peace and Conflict module visualizations" pending a real fix once the Action's logs reveal the actual field names.
- 2026-09-11 (later still) — Root-caused and fixed the shape mismatch flagged above: field names were fine all along (confirmed via the [diag] logs), the real bug was UNHCR's default 100-row page cap, compounded in `fetchDisplacement()` by `coo_all=false&coa_all=false` not server-aggregating into one global row as docs implied. Both fetchers now request `limit=1000` and sum client-side over per-origin-country rows; diagnostic logging kept in place to confirm the cap actually lifts on the next real run (if row counts stay ~100, real pagination is the follow-up). Logged under "Peace and Conflict module visualizations."
- 2026-09-11 (later still) — Recolored the Distributional Justice (Gini) panel chart from sage to ochre, by request, to signal a rising Gini ratio as a normatively "bad" trend using the ticker's existing polarity-scheme hue. Flagged as a deliberate departure from the one-token-per-pillar rule the chart previously followed — it no longer color-matches the other three Pillar 2 ticker indicators — and it deepens the existing ochre collision. Logged under "Distributional Justice module visualizations" and Style.
- 2026-09-11 (later still) — Found and fixed a decisions-vs-implementation gap: prior entries described Panel 3's Lorem-ipsum placeholder as already replaced with the region-breakdown bar chart, but `index.html` still had the bare placeholder — only the backend (`fetchDisplacementByRegion()`, `peace-data.json`) had actually shipped. Added the missing front-end chart (horizontal bars, ristra, sorted descending by region) with a client-side minimum-region-count check mirroring the fetcher's own guard, and corrected the footer's live-pipeline list, which had also omitted UNHCR entirely. Logged under "Peace and Conflict module visualizations."
- 2026-09-11 (later still) — `limit=1000` raised UNHCR's per-request row cap as intended (confirmed via [diag] logs: counts went from ~100 to exactly 1000), but `fetchDisplacement()`'s global total was still implausibly low (15.0M) because the real result set spans more than one page. Added a shared `fetchAllUnhcrRows()` pagination helper (loops via a `page` param until a page returns fewer rows than requested, with a 50-page safety ceiling and a same-page-repeated bailout in case `page` turns out to be unsupported) and wired both UNHCR fetchers to it. Also added a [50M, 300M] magnitude check to `fetchDisplacementByRegion()`, which previously only checked region count — a gap that could have let the same undercount pass silently as long as it spanned 3+ regions. Logged under "Peace and Conflict module visualizations."
